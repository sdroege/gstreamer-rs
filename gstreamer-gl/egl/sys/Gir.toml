[options]
library = "GstGLEGL"
min_cfg_version = "1.14"
version = "1.0"
work_mode = "sys"

extra_versions = [
    "1.20",
    "1.18",
    "1.16",
]

external_libraries = [
    "GLib",
]

generate = [
    "GstGLEGL.GLDisplayEGL",
]

[external_libraries]
gstreamer-gl = "GstGL"
